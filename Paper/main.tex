%% ============================================================
%%  Crime, Law and Social Change  |  Springer  |  ISSN 0925-4994
%%  Special Issue: AI Enabled Security Frameworks for
%%                 Emerging Cybercrime and Digital Threats
%%
%%  Self-contained: bibliography embedded via thebibliography.
%%  Compile on Overleaf (blank project) with pdfLaTeX.
%%  Compile order: pdflatex -> pdflatex  (no bibtex needed)
%% ============================================================
\documentclass[12pt,a4paper]{article}

%% ── Page geometry ────────────────────────────────────────────
\usepackage[top=2.5cm,bottom=2.5cm,left=3cm,right=3cm]{geometry}

%% ── Fonts and encoding ───────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}

%% ── Mathematics ──────────────────────────────────────────────
\usepackage{amsmath,amssymb,amsthm}

%% ── Graphics ─────────────────────────────────────────────────
\usepackage{graphicx}
\usepackage{subcaption}
\graphicspath{{figures/}}

%% ── Tables ───────────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{tabularx}
\usepackage{array}

%% ── Section formatting ───────────────────────────────────────
\usepackage{titlesec}
\titleformat{\section}{\normalfont\large\bfseries}{\thesection}{1em}{}
\titleformat{\subsection}{\normalfont\normalsize\bfseries}{\thesubsection}{1em}{}
\titleformat{\subsubsection}{\normalfont\normalsize\itshape}{\thesubsubsection}{1em}{}

%% ── Header / footer ──────────────────────────────────────────
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\textit{Crime, Law and Social Change}}
\fancyhead[R]{\small\textit{Korde et al.: MixTrace CoinJoin Deanonymization}}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}

%% ── Hyperlinks ───────────────────────────────────────────────
\usepackage[colorlinks=true,linkcolor=blue,
            citecolor=blue,urlcolor=blue,
            pdftitle={MixTrace: Forensic Detection of Illicit CoinJoin Transactions},
            pdfauthor={Sagar Korde et al.}]{hyperref}
\usepackage{url}
\usepackage{xurl}          % allows line-breaks anywhere in URLs/DOIs
\usepackage[authoryear,round]{natbib}  % author-year citations: \citet / \citep

%% ── Line spacing ─────────────────────────────────────────────
\usepackage{setspace}
\onehalfspacing


%% ── Abstract formatting ──────────────────────────────────────
\usepackage{abstract}
\renewcommand{\abstractnamefont}{\normalfont\bfseries}
\renewcommand{\abstracttextfont}{\normalfont\small\onehalfspacing}

%% ── Footnote for affiliations ────────────────────────────────
\usepackage{authblk}

%% ── Algorithm pseudo-code ────────────────────────────────────
\usepackage{algorithm}
\usepackage{algpseudocode}
\algrenewcommand\algorithmicrequire{\textbf{Input:}}
\algrenewcommand\algorithmicensure{\textbf{Output:}}

%% ── SVG figures ──────────────────────────────────────────────
\usepackage{svg}            % \includesvg — requires Inkscape on Overleaf

%% ── Custom operators ─────────────────────────────────────────
\DeclareMathOperator{\Entropy}{H}

% ─────────────────────────────────────────────────────────────
%  Author and Affiliation block
% ─────────────────────────────────────────────────────────────
\title{\textbf{MixTrace: Graph Attention Network-Based
       Forensic Detection of Illicit CoinJoin Transactions
       in the Bitcoin Blockchain}}

\author[1]{Sagar Korde}
\author[1]{Anuprita Korde}
\author[1]{Ashwini Dalvi}
\author[2]{Abhijeet Pasi}
\author[1]{Irfan Siddavatam}

\affil[1]{\small Department of Information Technology,
          K.\,J. Somaiya School of Engineering,
          Somaiya Vidyavihar University,
          Vidyavihar, Mumbai, India}

\affil[2]{\small Department of Computer Engineering,
          K.\,J. Somaiya School of Engineering,
          Somaiya Vidyavihar University,
          Vidyavihar, Mumbai, India}

\date{\small Submitted: \today}

% ─────────────────────────────────────────────────────────────
\begin{document}
% ─────────────────────────────────────────────────────────────
\maketitle
\thispagestyle{fancy}

% ─────────────────────────────────────────────────────────────
%  ABSTRACT
% ─────────────────────────────────────────────────────────────
\begin{abstract}
\noindent
Bitcoin CoinJoin transactions merge the unspent outputs of
unrelated users into a single on-chain record, systematically
undermining the Common Input Ownership Heuristic relied upon by
financial intelligence units and exchange compliance teams.
Three compounding limitations afflict existing detection tools:
multi-input clustering fails under script-type
indistinguishability, spending-transaction linking exploits only
one-dimensional temporal proximity, and graph classifiers perform
poorly on severely imbalanced illicit-transaction benchmarks.

The present study introduces MixTrace, a multi-modal framework
addressing all three limitations. A structural DBSCAN clustering
strategy, evaluated on multi-input P2WPKH transactions as a
pre-Taproot proxy, reduces the false-positive rate by 79.8
percent across 200,000 transactions, eliminating over 5,200
unjustified attributions. This gain
carries a false-negative rate of 70.0 percent, a deliberate
high-precision operating point suited to contexts where erroneous
attributions trigger account freezes or Suspicious Activity
Reports. A four-dimensional Chamfer Distance formulation shows
no measurable improvement over the temporal baseline on the
present dataset, identifying a boundary condition for future
work. A Deep Graph Infomax pre-training and Graph Attention
Network pipeline achieves an illicit-class F1-score of 0.7337
and ROC-AUC of 0.9868 on the Elliptic benchmark, outperforming
Random Forest (F1: 0.6285) and a GAT without pre-training
(F1: 0.2037). Self-supervised pre-training on unlabelled
blockchain nodes contributes 36 percentage points of the
final F1-score.

The results carry direct implications for FATF-compliant
transaction monitoring, the proportionality of automated
financial surveillance, and the evidentiary admissibility of
graph-based blockchain evidence in criminal proceedings.

\smallskip\noindent\textbf{Keywords:} Bitcoin;
Blockchain forensics;
CoinJoin;
Deep Graph Infomax;
Graph Attention Network;
Illicit transaction detection
\end{abstract}

% ─────────────────────────────────────────────────────────────
\section{Introduction}
\label{sec:intro}
% ─────────────────────────────────────────────────────────────

The rapid proliferation of cryptocurrency-facilitated crime
has placed blockchain forensics at the centre of contemporary
financial intelligence practice. Anti-money laundering
researchers have documented systematic weaknesses in current
blockchain monitoring tools that struggle with large-scale
illicit fund tracing~\citep{lo2023}, and the integration of
federated learning approaches for cryptocurrency fraud
detection has identified similar gaps in distributed
forensic systems~\citep{ahmed2024fed}. A systematic review of
machine learning methods for financial fraud detection
covering 94 primary studies identifies class imbalance,
temporal graph evolution, and exploitation of unlabelled data
as the three most critical open challenges~\citep{motie2024}.

Bitcoin operates on a transparent public ledger through a
pseudonymous unspent transaction output (UTXO) model in
which every transaction maps a set of input addresses to
output addresses. CoinJoin~\citep{ouyang2024btc} disrupts the
primary clustering heuristic used by investigators by enabling
multiple independent parties to collaboratively construct a
single transaction that merges their UTXOs, severing the
fund-flow trail that the Common Input Ownership Heuristic
(CIOH) relies upon. Quantitative analysis of CoinJoin
participation in the Bitcoin network confirms that multi-round
mixing substantially reduces CIOH clustering
confidence~\citep{coinjoin2024tdsc}. Privacy-focused wallets
including Wasabi and JoinMarket have processed hundreds of
millions of dollars through CoinJoin coordination, making
automated deanonymization a statutory priority for financial
intelligence units and exchange compliance teams.

Three research gaps persist in the current literature that
collectively limit the practical effectiveness of automated
CoinJoin analysis.

\textbf{Gap~1 -- Multi-Input Clustering Under Script
Indistinguishability.}
Bitcoin's Taproot soft fork (block height 709,632, November
2021) introduced Pay-to-Taproot (P2TR) scripts based on
Schnorr signatures and key aggregation. A single-user P2TR
output is computationally indistinguishable on-chain from a
multi-party aggregated output used in CoinJoin coordination,
causing naive CIOH clustering to generate systematic false
positives~\citep{lee2024}. Even before Taproot, multi-input
P2WPKH transactions share structural properties with
CoinJoin outputs, making the clustering problem inherent to
the UTXO model rather than exclusive to a single script
type~\citep{asiri2025}. The present study evaluates the
structural clustering solution on pre-Taproot P2WPKH
proxies; validation on live P2TR transactions is identified
as a necessary future extension.

\textbf{Gap~2 -- One-Dimensional CST Linking.}
CoinJoin Spending Transaction (CST) deanonymization requires
linking the outputs of a mixing event to the subsequent
spending transactions of participants. The only published
similarity metric for this task applies a Chamfer distance
over timestamps alone~\citep{chen2025multi}, leaving fee-rate
behaviour, input multiplicity, and output cardinality as
unexploited structural signals.

\textbf{Gap~3 -- Class Imbalance in Graph-Based Detection.}
The Elliptic Bitcoin Dataset~\citep{fan2024gnn}, the canonical
benchmark for illicit node detection, contains 4,545 illicit
and 42,019 licit labelled transactions, a 9.2:1
licit-to-illicit imbalance. Graph classifiers suppress
minority-class predictions to maximise overall accuracy,
producing illicit F1-scores below 0.50 in published
configurations that apply neither self-supervised pre-training
nor class-specific loss weighting~\citep{alarab2024}. The 157,205 unlabelled
nodes represent structural information that supervised
methods discard.

To address all three gaps, the present study introduces
\textbf{MixTrace}, comprising: (i)~a structural DBSCAN
clustering strategy on multi-dimensional transaction features,
evaluated on pre-Taproot P2WPKH proxies as the Gap~1
validation corpus;
(ii)~a Multi-Dimensional Chamfer Distance (MD-CD) formulation
extending CST linking to four dimensions; and (iii)~a
two-phase graph learning pipeline applying Deep Graph Infomax
(DGI) self-supervised pre-training before supervised Graph
Attention Network (GAT) fine-tuning with class-weighted
cross-entropy and ADASYN oversampling.

Experiments on the public Elliptic Bitcoin Dataset and an
author-curated corpus of 5,884,387 Bitcoin transactions
demonstrate that MixTrace achieves an illicit-class F1-score
of 0.7337 and ROC-AUC of 0.9868, representing 10.5 percentage
points improvement over the strongest conventional baseline
and a 79.8 percent relative reduction in clustering
false-positive rate (absolute: 2.97 percentage points).

The manuscript proceeds as follows: Section~\ref{sec:related}
reviews the literature; Section~\ref{sec:method} presents
the methodology; Section~\ref{sec:exp} describes experimental
setup and results; Section~\ref{sec:discuss} analyses the
findings; and Section~\ref{sec:conclude} summarises
contributions and identifies future directions.

% ─────────────────────────────────────────────────────────────
\section{Related Work}
\label{sec:related}
% ─────────────────────────────────────────────────────────────

\subsection{Graph Neural Networks for Blockchain Forensics}

Graph neural network architectures have become the dominant
paradigm for illicit transaction detection on public
blockchains. \citet{asiri2025} demonstrated that multi-hop
neighbourhood aggregation via GCN on Bitcoin transaction
graphs captures structural patterns invisible to
transaction-level feature classifiers. A recurrent GCN
approach by~\citet{alarab2024} integrates sequential temporal
modelling with spatial message passing, achieving improved
stability on the Elliptic benchmark. Bitcoin anti-money
laundering has been framed as a subgraph contrastive learning
problem by~\citet{ouyang2024btc}, who construct positive
and negative subgraph pairs to train representations that
cluster illicit transaction neighbourhoods distinctly from
licit flows. A multi-distance spatial-temporal GNN for
blockchain anomaly detection that exploits transaction timing
at multiple temporal resolutions was introduced
by~\citet{chen2025multi}, providing a complementary
multi-scale temporal baseline to the single-resolution
Chamfer distance used in the present study. The global-local
graph attention framework of~\citet{globalloc2025} combines
local multi-head attention with a global pooling context,
using cyclic pseudo-labelling to propagate supervisory signal
into the unlabelled majority.

\subsection{Anti-Money Laundering with Machine Learning}

A systematic review by~\citet{motie2024} of 94 primary
studies on GNNs for financial fraud identification concludes
that attention-based neighbourhood aggregation consistently
outperforms isotropic mean aggregation when fraudulent and
benign subgraph patterns differ structurally. A focused review
by~\citet{fan2024gnn} further confirms that self-supervised
auxiliary objectives improve downstream classification F1
across imbalanced blockchain benchmarks. Balanced-BiEGCN,
a bidirectional evolving GCN with class-balanced learning
for dynamic Bitcoin anomaly detection, was proposed
by~\citet{xiao2025}, addressing temporal label drift across
the Elliptic dataset's 49-time-step span. GCN-based hyperedge
classification that groups Bitcoin transactions into
operation-level hyperedges before assigning illicit scores
was introduced as CENSor by~\citet{lee2024}, achieving
precision improvements over node-level classifiers. A
comprehensive benchmark of machine learning methods for
blockchain anti-money laundering conducted by~\citet{lo2023}
established that graph-structural features derived from the
directed edge list provide the most consistent performance
improvement across all evaluated classifier families.

\subsection{CoinJoin Privacy and Deanonymization}

Quantitative study of CoinJoin participation in the Bitcoin
network by~\citet{coinjoin2024tdsc} measured the reduction
in CIOH clustering confidence as a function of mixing round
count and wallet-level behaviour. Anomaly detection methods
applied to cryptocurrency networks were reviewed
by~\citet{futureinternet2025}, who identified DBSCAN and
isolation-forest variants as the most effective unsupervised
baselines for exchange-level transaction flagging. Structural
properties of non-standard Bitcoin scripts including
multi-signature coordination patterns that overlap with
CoinJoin outputs were examined by~\citet{kiani2024}, noting
that post-Taproot script type convergence creates
classification challenges for rule-based detection systems.
Data augmentation combined with hybrid GNN architectures for
phishing detection on Ethereum demonstrated by~\citet{chen2024}
that reducing structural overlap between legitimate and
malicious transaction patterns substantially lowers false
positives, a finding directly applicable to Taproot
indistinguishability. A transformer-based traversal of
transaction graphs by~\citet{choi2024} improved phishing
scam detection by capturing long-range transaction path
dependencies that local message-passing GNNs miss.

\subsection{Self-Supervised Graph Representation Learning}

Deep Graph Infomax (DGI)~\citep{velickovic2019dgi} frames
graph representation learning as a mutual information
maximisation problem between local node embeddings and a
global graph summary, enabling pre-training on unlabelled
graph data. Disentangled prototypical autoencoder learning
on unlabelled cryptocurrency transaction nodes
by~\citet{graphanomaly2024} demonstrated that self-supervised
prototype representations improve classifier calibration by
up to 40 percentage points on severely imbalanced benchmarks,
validating the DGI pre-training strategy in the present study.
A temporal-aware heterogeneous graph oversampling strategy
for Internet fraud detection combining attention-based
neighbourhood fusion with synthetic minority sample generation
was proposed by~\citet{wei2025}, simultaneously addressing
class imbalance and temporal heterogeneity. Privacy-preserving
collaborative training across multiple exchange nodes that
maintains detection accuracy competitive with centralised
graph models was demonstrated for cryptocurrency fraud
by~\citet{ahmed2024fed}. The interaction between federated
aggregation and class-balanced loss functions was analysed
by~\citet{baabdullah2024}, showing that federated training
preserves class-weighting effectiveness when participating
clients share similar imbalance ratios.

\subsection{Class Imbalance Mitigation in Fraud Detection}

An advancement of the ADASYN algorithm with density-sensitive
synthesis boundaries by~\citet{tekkali2024} demonstrated
improved F1-score on fraud detection benchmarks with imbalance
ratios up to 100:1. ADASYN combined with SHAP-based feature
selection evaluated by~\citet{creditadasyn2024} showed that
selective feature retention before oversampling reduces
synthetic sample noise and improves classifier calibration.
A comparative study of traditional and deep learning models
under class imbalance mitigation strategies by~\citet{albalawi2025}
concluded that inverse-frequency class weighting within the
loss function produces the most stable precision-recall
trade-off for neural network architectures, justifying the
primary imbalance strategy adopted in the present study.
XAI-enabled ensemble stacking for Ethereum anomaly
detection by~\citet{chithanuru2025} and stacking ensembles
for Ethereum fraud by~\citet{gu2025} both confirmed that
combining minority-class oversampling during training with
threshold optimisation at inference substantially improves
F1-score on imbalanced blockchain benchmarks.

\subsection{Explainability and Near-Real-Time Detection}

A near-real-time Ethereum fraud detection framework with
integrated gradient-based attribution analysis
by~\citet{ethereum2025xai} identified transaction fee and
timing features as primary illicit classification drivers in
GCN models, a finding consistent with the novel temporal
irregularity feature engineered in the present study.
The temporal-aware approach by~\citet{wei2025} further
validated that multi-relational graph encoding captures
complementary signals from address-to-transaction and
transaction-to-address edge types, motivating the
bidirectional edge construction used in the MixTrace graph
object.

% ─────────────────────────────────────────────────────────────
\section{Methodology}
\label{sec:method}
% ─────────────────────────────────────────────────────────────

\subsection{Problem Formulation}
\label{sec:problem}

Let $\mathcal{G} = (\mathcal{V}, \mathcal{E}, \mathbf{X})$
denote the Elliptic transaction graph, where $\mathcal{V}$
is the set of $N = 203{,}769$ transaction nodes,
$\mathcal{E}$ the set of 468,710 bidirectional edges
representing Bitcoin payment flows (derived from 234,355
directed edges), and $\mathbf{X} \in \mathbb{R}^{N \times 169}$
the node feature matrix formed by augmenting 165 original
Elliptic features with four novel graph-structural features.
Each node $v$ carries a time step $\tau(v) \in \{1,\ldots,49\}$
and label $y_v \in \{1, 0, -1\}$, where 1 denotes illicit,
0 denotes licit, and $-1$ denotes unknown.
The labelled subset comprises 46,564 nodes (4,545 illicit
and 42,019 licit); the remaining 157,205 are unlabelled.
The learning objective is binary node classification
evaluated on nodes with $\tau(v) \in \{42,\ldots,49\}$.

Figure~\ref{fig:architecture} provides an overview of the
complete MixTrace framework, illustrating how each of the
three research gaps is addressed by a dedicated module and
how the modules are evaluated independently against their
respective datasets.

\begin{figure}[htbp]
  \centering
  \includesvg[width=\linewidth]{fig00_architecture}
  \caption{MixTrace multi-modal forensic framework
    architecture. The framework addresses three independent
    research gaps through parallel evaluation tracks.
    \textbf{Gap~1} (top, green): structural DBSCAN
    clustering on a six-dimensional feature vector reduces
    the false-positive rate from 3.72\,\% to 0.75\,\% across
    200,000 multi-input P2WPKH transactions.
    \textbf{Gap~2} (middle, amber): Multi-Dimensional Chamfer
    Distance linking on four-dimensional temporal window
    features yields a null result (MD-CD F1 = 0.245 vs.\
    1-D F1 = 0.251), identifying a boundary condition.
    \textbf{Gap~3} (bottom, blue): a two-phase DGI
    pre-training and GAT fine-tuning pipeline on the Elliptic
    benchmark achieves illicit-class F1 = 0.7337 and
    ROC-AUC = 0.9868; the dashed arrow denotes the frozen
    skip-connection $[\mathbf{H}_2 \,\|\, \mathbf{Z}^*]$
    from DGI embeddings into the GAT fine-tuning phase.}
  \label{fig:architecture}
\end{figure}

\subsection{Novel Feature Engineering}
\label{sec:features}

Nine novel features supplement existing representations
across both experimental datasets.

\subsubsection{Author Dataset: Five Transaction-Level Features}

\paragraph{Denomination Entropy.}
CoinJoin transactions characteristically produce multiple
outputs of equal value to obscure the payment-to-change
distinction. For transaction $t$ with output value vector
$\mathbf{v} = (v_1,\ldots,v_k)$, define the normalised
probability vector $p_i = v_i / \sum_j v_j$.
Equation~\eqref{eq:entropy} computes the Shannon entropy
of the UTXO output value distribution, quantifying how
uniformly transaction value is spread across outputs.

\begin{equation}
  \Entropy(t) = -\sum_{i=1}^{k} p_i \log_2 p_i.
  \label{eq:entropy}
\end{equation}

High entropy ($\Entropy \to \log_2 k$) signals equal-valued
outputs, a structural CoinJoin signature.

\paragraph{Mixing Index.}
Equation~\eqref{eq:mixing} measures the combinatorial
ambiguity of input-output pairing, providing a proxy for
how many possible sender-receiver linkages exist within
a single transaction.

\begin{equation}
  \text{MI}(t) = \frac{|\mathcal{A}| \cdot |\mathcal{B}|}
                      {|\mathcal{A} \cup \mathcal{B}|},
  \label{eq:mixing}
\end{equation}

where $\mathcal{A}$ and $\mathcal{B}$ are the input and
output address sets. The denominator $|\mathcal{A} \cup
\mathcal{B}|$ normalises by the number of distinct addresses
involved, yielding a per-address combinatorial mixing
measure. For a standard single-input, single-output payment
where $\mathcal{A}$ and $\mathcal{B}$ are disjoint singletons,
$\text{MI} = 1/2$; for a 10-input, 10-output CoinJoin
transaction with fully disjoint address sets,
$\text{MI} = 100/20 = 5$, growing proportionally with
combinatorial ambiguity. In the author dataset, MI has a
mean of 8.79 and standard deviation of 689.27.

\paragraph{Temporal Irregularity.}
Mixing coordination services schedule transactions at
atypical hours to reduce mempool competition.
Equation~\eqref{eq:ti} represents the $z$-score deviation
of a transaction's fee rate within the hour-of-day and
day-of-week cell, capturing scheduling anomalies relative
to normal fee-rate behaviour.

\begin{equation}
  \text{TI}(t) = \frac{f_t - \mu_{h,d}}{\sigma_{h,d}},
  \label{eq:ti}
\end{equation}

where $f_t$ is the fee rate in sat/vbyte and $\mu_{h,d}$,
$\sigma_{h,d}$ are the cell mean and standard deviation.

\paragraph{UTXO Concentration.}
Equation~\eqref{eq:uc} approximates the Herfindahl-Hirschman
Index over output values, measuring how concentrated the
transaction value is in a single output versus distributed
across many.

\begin{equation}
  \text{UC}(t) = r^2,
  \label{eq:uc}
\end{equation}

where $r = \max_i v_i \,/\, \sum_j v_j$ is the proportion
of total transaction value carried by the single largest
output. CoinJoin outputs, which equalise values across
multiple outputs, produce UC near zero; single-payment
transactions yield UC near one.

\paragraph{CoinJoin Proximity Score.}
Equation~\eqref{eq:cps} aggregates weighted rule-based
CoinJoin structural indicators into a single composite
score, providing a human-interpretable heuristic baseline
feature alongside the learned representations.

\begin{equation}
  \text{CPS}(t) = \sum_{r \in \mathcal{R}} w_r
                  \cdot \mathbf{1}[t \models r],
  \label{eq:cps}
\end{equation}

where $\mathcal{R}$ includes equal-denomination output
flags, input count exceeding three, zero address reuse,
and round output values. The mean CPS is 0.2663 with
standard deviation 0.1307.

\subsubsection{Elliptic Dataset: Four Graph-Structural Features}

\paragraph{Graph In-Degree and Out-Degree.}
In-degree and out-degree are computed from the directed
edge list, with mean values of 1.150 and maximum values
of 284 and 472 respectively, indicating a strongly
skewed hub structure consistent with exchange and
darknet-market transaction patterns.

\paragraph{Illicit Neighbour Ratio.}
Equation~\eqref{eq:inr} computes the fraction of labelled
one-hop neighbours carrying the illicit label, encoding
the guilt-by-association principle that fraudulent
transactions cluster in connected subgraphs.

\begin{equation}
  \text{INR}(v) = \frac{\sum_{u \in \mathcal{N}(v)}
                  \mathbf{1}[y_u = 1]}
                  {|\{u \in \mathcal{N}(v):y_u \neq -1\}|}.
  \label{eq:inr}
\end{equation}

INR achieves a Pearson correlation of $r = +0.4159$ with
the ground-truth label.

\paragraph{Shortest Path to Illicit.}
A breadth-first search from each confirmed illicit source
computes the minimum hop distance to every reachable node,
capped at six. To prevent any label leakage into the
evaluation partition, source nodes are restricted to
confirmed illicit transactions from training time steps
1--34 only; test-partition nodes at time steps 42--49
contribute no source labels. The feature is therefore
derived exclusively from training-split ground truth,
maintaining the integrity of the chronological evaluation
protocol. This feature achieves a Pearson correlation
of $r = -0.9182$ with the illicit label, the strongest
single-feature predictor in the augmented set, consistent
with the financial fraud network proximity analysis
documented by~\citet{gu2025}.

Figure~\ref{fig:feat_imp} shows the top-ranked features
by importance score derived from the Random Forest
classifier, illustrating that the novel graph-structural
features rank among the highest discriminative contributors
in the final feature set.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.85\linewidth]{fig01_feature_importance.png}
  \caption{Top feature importance scores from the Random
    Forest classifier trained on the augmented 169-dimensional
    feature set. This figure shows that the novel
    shortest-path-to-illicit and illicit-neighbour-ratio
    features rank among the highest discriminative contributors,
    validating the feature engineering step in the MixTrace
    pipeline.}
  \label{fig:feat_imp}
\end{figure}

\subsection{Multi-Dimensional Chamfer Distance}
\label{sec:mdcd}

Equation~\eqref{eq:chamfer} defines the one-sided Chamfer
distance from point set $\mathcal{U}$ to point set
$\mathcal{V}$ in $\mathbb{R}^d$. This equation represents
the mean nearest-neighbour distance from each source point
to any target point, measuring asymmetric distributional
proximity between two transaction window feature sets.

\begin{equation}
  d_C(\mathcal{U},\mathcal{V})
    = \frac{1}{|\mathcal{U}|}
      \sum_{u_i \in \mathcal{U}}
      \min_{v_j \in \mathcal{V}} \|u_i - v_j\|_2.
  \label{eq:chamfer}
\end{equation}

The baseline 1-D formulation uses only the Unix timestamp
($d=1$). The MD-CD sets $d=4$ with the standardised feature
vector defined in Equation~\eqref{eq:phi}. This equation
represents the joint structural feature encoding for a
transaction $t$, combining temporal positioning with
fee-rate behaviour and topological complexity.

\begin{equation}
  \phi(t) = \bigl[
    \text{timestamp},\;
    \text{fee\_rate},\;
    \text{input\_count},\;
    \text{output\_count}
  \bigr]^\top.
  \label{eq:phi}
\end{equation}

Each dimension is independently normalised to zero mean
and unit variance. Transactions are grouped into one-hour
temporal windows indexed by block height divided by six.
A candidate window pair $(W_i, W_j)$ is predicted as a
linked CoinJoin-spend pair if $d_C(W_i, W_j) < \theta^*$,
where $\theta^*$ maximises F1-score on the validation set.

\subsection{Structural DBSCAN Clustering}
\label{sec:taproot_method}

\subsubsection{Naive CIOH Baseline}

Equation~\eqref{eq:cioh} encodes the naive Common Input
Ownership Heuristic prediction rule, which flags every
transaction exceeding input and output count thresholds
as a CoinJoin candidate regardless of script type.

\begin{equation}
  \hat{y}_\text{CIOH}(t)
    = \mathbf{1}[|\mathcal{A}| > 2]
    \cdot \mathbf{1}[|\mathcal{B}| > 2].
  \label{eq:cioh}
\end{equation}

This rule achieves perfect recall but accumulates false
positives that scale with P2TR adoption.

\subsubsection{Structural DBSCAN Clustering}

The proposed method clusters transactions using
DBSCAN~\citep{ester1996} on the six-dimensional structural
feature vector defined in Equation~\eqref{eq:psi}.
This equation represents the combined structural behaviour
profile of a transaction, encoding UTXO topology,
fee-rate positioning, and mixing-related indicators.

\begin{multline}
  \psi(t) = \bigl[
    \text{input\_count},\;
    \text{output\_count},\;
    \text{fee\_rate},\\
    \text{value\_concentration},\;
    \text{address\_reuse},\;
    \text{mixing\_index}
  \bigr]^\top.
  \label{eq:psi}
\end{multline}

All features are standardised before clustering. DBSCAN
is configured with $\varepsilon = 0.6$ and
$\text{min\_samples} = 5$ using a $k$-d tree index.
To address the $O(n^2)$ memory complexity at dataset scale,
DBSCAN is fitted on a stratified subsample of 30,000
transactions; cluster labels are propagated to the full
200,000-transaction analysis set via nearest-centroid
assignment within the $\varepsilon$ radius. Points falling
outside all cluster radii receive the naive CIOH prediction
as fallback. Cluster CoinJoin labels are assigned by
majority vote over ground-truth labels of cluster members.

\subsection{MixTrace: DGI Pre-Training and GAT Fine-Tuning}
\label{sec:mixtrace_method}

\subsubsection{Phase 1: Deep Graph Infomax Pre-Training}

The MixTrace graph encoder~\citep{velickovic2019dgi} is a
two-layer GCN that maps the node feature matrix to an
embedding space. Equation~\eqref{eq:gcn1} represents the
first-layer GCN transformation, applying the normalised
adjacency to project input features to a 256-dimensional
hidden representation.

\begin{equation}
  \mathbf{H}^{(1)} = \text{ELU}\!\bigl(
    \hat{\mathbf{A}}\,\mathbf{X}\,\mathbf{W}_1\bigr)
    \in \mathbb{R}^{N \times 256},
  \label{eq:gcn1}
\end{equation}

where $\hat{\mathbf{A}} = \tilde{\mathbf{D}}^{-1/2}
\tilde{\mathbf{A}}\tilde{\mathbf{D}}^{-1/2}$ is the
symmetrically normalised adjacency matrix with self-loops.
Equation~\eqref{eq:gcn2} represents the second-layer GCN
transformation, compressing the hidden representation to
the final 128-dimensional node embedding space.

\begin{equation}
  \mathbf{Z} = \text{ELU}\!\bigl(
    \hat{\mathbf{A}}\,\mathbf{H}^{(1)}\,\mathbf{W}_2\bigr)
    \in \mathbb{R}^{N \times 128}.
  \label{eq:gcn2}
\end{equation}

The global graph summary is $\mathbf{s} =
\sigma(\text{mean}_v\,\mathbf{z}_v) \in \mathbb{R}^{128}$.
A bilinear discriminator $\mathcal{D}(\mathbf{z}_i,
\mathbf{s}) = \mathbf{z}_i^\top \mathbf{W}_d\, \mathbf{s}$
scores node-summary agreement. Corrupted embeddings
$\tilde{\mathbf{Z}}$ are produced by row-shuffling
$\mathbf{X}$. Equation~\eqref{eq:dgi} defines the binary
cross-entropy DGI training objective, which maximises
agreement scores for real node-summary pairs and minimises
them for corrupted pairs.

\begin{equation}
  \mathcal{L}_\text{DGI} = -\frac{1}{2N}
    \sum_{i=1}^{N}\!\bigl[
      \log\sigma\!\bigl(\mathcal{D}(\mathbf{z}_i,
        \mathbf{s})\bigr)
      + \log\!\bigl(1 - \sigma\!\bigl(
          \mathcal{D}(\tilde{\mathbf{z}}_i,
          \mathbf{s})\bigr)\bigr)
    \bigr].
  \label{eq:dgi}
\end{equation}

Pre-training runs for 300 epochs using Adam with learning
rate $10^{-3}$ and weight decay $5 \times 10^{-4}$.
Figure~\ref{fig:dgi_loss} shows the DGI training loss
progression over 300 epochs, illustrating the convergence
behaviour of the encoder-discriminator system and
confirming stable learning throughout pre-training.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.75\linewidth]{fig02_dgi_loss.png}
  \caption{DGI training loss over 300 pre-training epochs.
    This figure shows the binary cross-entropy loss defined
    in Equation~\eqref{eq:dgi} converging from an initial
    value of 1.459 at epoch 1 to stable low values by
    epoch 300, confirming that the encoder successfully
    learns to discriminate real node-summary pairs from
    corrupted pairs across the full 203,769-node graph.}
  \label{fig:dgi_loss}
\end{figure}

\subsubsection{Phase 2: Graph Attention Network Fine-Tuning}

The MixTrace GAT architecture~\citep{velickovic2018gat}
processes the full 169-dimensional feature matrix through
two attention layers. The first layer uses eight parallel
attention heads with concatenated outputs; the second
uses a single head. After attention processing, Equation~\eqref{eq:fused}
defines the fused representation that concatenates the GAT
output with the frozen DGI embedding, combining
local attention-derived neighbourhood features with global
topology-derived structural context.

\begin{equation}
  \mathbf{H}_\text{fused}
    = [\mathbf{H}_2 \;\|\; \mathbf{Z}]
    \in \mathbb{R}^{N \times 256},
  \label{eq:fused}
\end{equation}

where $\mathbf{H}_2 \in \mathbb{R}^{N \times 128}$ is the
GAT second-layer output and $\mathbf{Z}$ is the
pre-computed frozen DGI embedding. A two-layer MLP
classifier maps the fused representation to logits
$\hat{\mathbf{y}} \in \mathbb{R}^{N \times 2}$.

\subsubsection{Training Protocol}

Equation~\eqref{eq:loss} defines the class-weighted
cross-entropy loss function, which penalises illicit
misclassifications 4.317 times more heavily than licit
misclassifications to counteract the 9.2:1 imbalance ratio.

\begin{equation}
  \mathcal{L}_\text{GAT}
    = -\sum_{v \in \mathcal{V}_L}
      w_{y_v} \log \hat{p}(y_v \mid v),
  \label{eq:loss}
\end{equation}

where $\mathcal{V}_L$ is the labelled training set,
$w_0 \approx 0.565$ (licit), and $w_1 \approx 4.317$
(illicit). The optimiser is Adam with initial learning
rate $5 \times 10^{-4}$ and weight decay $5 \times 10^{-4}$.
A ReduceLROnPlateau scheduler monitors validation F1
with patience 10 and factor 0.5. Training terminates
after 500 epochs or 20 consecutive epochs without
validation improvement. ADASYN oversampling of the
labelled training feature matrix is applied as a
supplementary diagnostic, following the density-sensitive
synthesis protocol documented by~\citet{tekkali2024}.

Algorithm~\ref{alg:mixtrace} summarises the complete
MixTrace training and inference procedure, consolidating
the DGI pre-training phase, GAT fine-tuning phase, and
threshold-calibrated inference into a single unified
specification.

\begin{algorithm}[htbp]
\caption{MixTrace Training and Inference}
\label{alg:mixtrace}
\begin{algorithmic}[1]
\Require Transaction graph
  $\mathcal{G}=(\mathcal{V},\mathcal{E},\mathbf{X})$,
  $\mathbf{X}\in\mathbb{R}^{N\times169}$,
  partial labels $\mathbf{Y}_L$, chronological split
  $\tau_\text{train}\!\in\!\{1,\ldots,34\}$,
  $\tau_\text{val}\!\in\!\{35,\ldots,41\}$,
  $\tau_\text{test}\!\in\!\{42,\ldots,49\}$
\Ensure Predicted labels $\hat{\mathbf{y}}$ for test nodes

\vspace{2pt}
\Statex \textit{--- Phase 1: DGI Unsupervised Pre-Training ---}
\For{$\text{epoch} \leftarrow 1$ \textbf{to} $300$}
  \State $\mathbf{H}^{(1)} \leftarrow
    \text{ELU}\!\bigl(\hat{\mathbf{A}}\mathbf{X}\mathbf{W}_1\bigr)
    \in \mathbb{R}^{N\times256}$
    \Comment{GCN layer 1}
  \State $\mathbf{Z} \leftarrow
    \text{ELU}\!\bigl(\hat{\mathbf{A}}\mathbf{H}^{(1)}\mathbf{W}_2\bigr)
    \in \mathbb{R}^{N\times128}$
    \Comment{GCN layer 2}
  \State $\mathbf{s} \leftarrow
    \sigma\!\bigl(\tfrac{1}{N}\textstyle\sum_v\mathbf{z}_v\bigr)$
    \Comment{global graph summary}
  \State $\tilde{\mathbf{X}} \leftarrow \text{shuffle\_rows}(\mathbf{X})$
    \Comment{row-shuffle corruption}
  \State $\tilde{\mathbf{Z}} \leftarrow
    \text{GCN\_encoder}(\tilde{\mathbf{X}})$
  \State Minimise $\mathcal{L}_\text{DGI}$ via Adam
    ($\text{lr}=10^{-3}$,\ $\lambda=5\times10^{-4}$)
\EndFor
\State $\mathbf{Z}^* \leftarrow \mathbf{Z}$
  \Comment{freeze DGI embeddings}

\vspace{2pt}
\Statex \textit{--- Phase 2: GAT Supervised Fine-Tuning ---}
\State best\_val\_F1 $\leftarrow 0$;\ no\_improve $\leftarrow 0$
\For{$\text{epoch} \leftarrow 1$ \textbf{to} $500$}
  \State $\mathbf{H}_1 \leftarrow
    \text{GAT}_1(\mathbf{X},\mathbf{A};\;\text{heads}=8)
    \in \mathbb{R}^{N\times128}$
  \State $\mathbf{H}_2 \leftarrow
    \text{GAT}_2(\mathbf{H}_1,\mathbf{A};\;\text{heads}=1)
    \in \mathbb{R}^{N\times128}$
  \State $\mathbf{H}_\text{fused} \leftarrow
    [\mathbf{H}_2 \;\|\; \mathbf{Z}^*]
    \in \mathbb{R}^{N\times256}$
    \Comment{skip connection}
  \State $\hat{\mathbf{y}} \leftarrow
    \text{MLP}(\mathbf{H}_\text{fused})$
  \State Minimise $\mathcal{L}_\text{GAT}$ with
    $w_0\!=\!0.565$, $w_1\!=\!4.317$
    \Comment{class-weighted CE}
  \If{val\_F1 improves}
    \State Save checkpoint;\ best\_val\_F1 $\leftarrow$ val\_F1;\ no\_improve $\leftarrow 0$
  \Else
    \State no\_improve $\leftarrow$ no\_improve $+1$
    \If{no\_improve $\bmod 10 = 0$}
      \State $\text{lr} \leftarrow \text{lr} \times 0.5$
        \Comment{ReduceLROnPlateau, factor 0.5}
    \EndIf
    \If{no\_improve $\geq 20$}
      \textbf{ break}
    \EndIf
  \EndIf
\EndFor
\State Load best checkpoint $\boldsymbol{\theta}^*$

\vspace{2pt}
\Statex \textit{--- Inference ---}
\State $\hat{p} \leftarrow
  \sigma\!\bigl(\text{MLP}_{\boldsymbol{\theta}^*}(
    [\text{GAT}(\mathbf{X}_\text{test})\,\|\,
     \mathbf{Z}^*_\text{test}])\bigr)$
\State $\theta^* \leftarrow \arg\max_\theta
  F_1(\hat{p} \geq \theta,\;\mathbf{Y}_\text{val})$
  \Comment{$\theta^*\!=\!0.9397$}
\State $\hat{\mathbf{y}} \leftarrow
  \mathbf{1}[\hat{p} \geq \theta^*]$
\State \Return $\hat{\mathbf{y}}$
\end{algorithmic}
\end{algorithm}

% ─────────────────────────────────────────────────────────────
\section{Experiments}
\label{sec:exp}
% ─────────────────────────────────────────────────────────────

\subsection{Datasets}
\label{sec:data}

\paragraph{Elliptic Bitcoin Dataset.}
The Elliptic dataset~\citep{weber2019} comprises 203,769
Bitcoin transaction nodes across 49 weekly time steps with
165 features: 93 local transaction features and 72
aggregated one-hop neighbourhood features. Class
distribution among 46,564 labelled nodes is 4,545 illicit
(9.76 percent) and 42,019 licit (90.24 percent), yielding
a 9.2:1 imbalance ratio. A strict chronological split
assigns time steps 1 to 34 to training (136,265 nodes),
35 to 41 to validation (30,680 nodes), and 42 to 49 to
the held-out test set (36,824 nodes), preventing temporal
data leakage. The test partition contains 408 illicit
and 8,433 licit labelled nodes. Structural analysis of this
dataset family, including its extended version, has been
conducted by~\citet{bellei2024}.

Figure~\ref{fig:class_dist} shows the class distribution
across labelled nodes in the Elliptic Bitcoin Dataset,
illustrating the severe 9.2:1 licit-to-illicit imbalance
that motivates the class-weighted loss function and
ADASYN diagnostic implemented in the MixTrace pipeline.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.75\linewidth]{fig03_class_distribution.png}
  \caption{Class distribution of labelled nodes in the
    Elliptic Bitcoin Dataset. This figure shows the
    4,545 illicit and 42,019 licit labelled transactions
    across 49 time steps, highlighting the 9.2:1
    licit-to-illicit imbalance ratio that serves as the
    central motivation for the class-weighted cross-entropy
    loss defined in Equation~\eqref{eq:loss} and the
    ADASYN oversampling diagnostic.}
  \label{fig:class_dist}
\end{figure}

\paragraph{Author-Curated Bitcoin Transaction Corpus.}
A corpus of 5,884,387 Bitcoin transactions with 53
engineered features including script-type indicators,
fee rates, input and output counts, address-reuse flags,
and value metrics was assembled. The ground-truth label
\texttt{is\_coinjoin\_like}, derived from structural
heuristics validated against known mixing-service patterns,
identifies 110,352 transactions (1.88 percent) as probable
CoinJoin participants. Because the corpus predates
widespread Pay-to-Taproot adoption, the dataset contains
zero confirmed P2TR transactions. Accordingly, the Gap 1
clustering analysis is conducted on multi-input P2WPKH
transactions as structural proxies that reproduce the
multi-owner coordination profile described in the
introduction, without the added Schnorr-signature
indistinguishability of post-Taproot outputs. The
clustering evaluation thus quantifies structural
discriminability under controlled pre-Taproot conditions;
validation on live P2TR data constitutes a necessary
future extension.

\subsection{Baseline Methods}

\textbf{K-Means} ($k=2$) applies Euclidean clustering to
the standardised node feature matrix with cluster-to-class
assignment via majority vote over training labels.

\textbf{K-Nearest Neighbours (KNN)} applies grid search
over $k \in \{3,5,7,11,15\}$ and uniform/distance
weighting, selecting the configuration maximising
validation illicit F1.

\textbf{Random Forest (RF)} applies grid search over
estimator count in $\{100,200\}$, maximum depth in
$\{10,20,\text{None}\}$, and minimum split samples in
$\{2,5\}$ with balanced class weighting.

\textbf{GAT without DGI} reproduces the full MixTrace
GAT architecture with the 128-dimensional DGI skip
connection replaced by a fixed zero tensor. The
downstream MLP classifier therefore receives a 256-dimensional
input whose upper half is uninformative, equivalent to
removing the pre-training contribution while preserving
all other architectural decisions. This design isolates
the DGI contribution without altering attention layer
structure or hyperparameters, consistent with ablation
methodology recommended by~\citet{motie2024}. A fully
separate GAT trained on 128-dimensional outputs alone
would require different MLP weights and a separate
hyperparameter search; the zero-tensor design holds all
other factors constant, making the DGI contribution the
sole experimental variable. This choice is acknowledged
as an approximation: the MLP classifier receives 256
inputs of which 128 are zero, and its weight matrix will
allocate zero gradient to the zeroed channels within two
to three epochs. The resulting model is functionally
equivalent to a 128-dimensional GAT-only classifier
after the first few training steps.

\subsection{Illicit Transaction Detection Results}
\label{sec:class_results}

Table~\ref{tab:classification} presents the complete
test-set classification performance for all evaluated
methods on the Elliptic held-out partition containing
408 illicit and 8,433 licit labelled nodes.

\begin{table}[htbp]
  \caption{Illicit transaction detection results on the
    Elliptic test set (time steps 42--49). This table presents
    precision, recall, illicit F1, macro F1, PR-AUC, and
    ROC-AUC for all methods. Best result per column appears
    in \textbf{bold}. All results are from a single run
    with fixed random seed (seed~42); multi-seed variance
    analysis is a planned extension.}
  \label{tab:classification}
  \centering
  \small
  \setlength{\tabcolsep}{5pt}
  \begin{tabular}{lcccccc}
    \toprule
    \textbf{Method}
      & \textbf{Prec.} & \textbf{Rec.}
      & \textbf{F1} & \textbf{F1\textsubscript{mac}}
      & \textbf{PR-AUC} & \textbf{ROC-AUC} \\
    \midrule
    K-Means
      & 0.046 & 1.000 & 0.088 & 0.045 & 0.000 & -- \\
    KNN
      & 0.499 & 0.468 & 0.483 & 0.729 & 0.416 & -- \\
    Random Forest
      & \textbf{0.946} & 0.471 & 0.628 & 0.808 & 0.632 & -- \\
    GAT (no DGI)
      & 0.113 & \textbf{0.995} & 0.204 & 0.486 & 0.269 & 0.932 \\
    \textbf{MixTrace (DGI+GAT)}
      & 0.709 & 0.760 & \textbf{0.734} & \textbf{0.860}
      & \textbf{0.717} & \textbf{0.987} \\
    \bottomrule
  \end{tabular}
\end{table}

MixTrace achieves the highest illicit F1-score (0.7337),
macro F1 (0.8602), PR-AUC (0.7167), and ROC-AUC (0.9868).
Random Forest attains the highest precision (0.9458) at the
cost of recall (0.4706). The standalone GAT without DGI
exhibits degenerate high-recall, low-precision behaviour
(recall 0.995, precision 0.113, F1 0.204), confirming that
graph attention layers alone collapse to near-unconditional
positive prediction without structural pre-training context
under the 9.2:1 imbalance ratio~\citep{alarab2024}.

Figure~\ref{fig:pr_curves} shows the precision-recall curves
for all methods, demonstrating that MixTrace maintains
superior precision across the full recall spectrum compared
to all baselines. Figure~\ref{fig:roc_curves} shows the
corresponding ROC curves, confirming MixTrace's dominant
ranking quality with ROC-AUC of 0.9868.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.85\linewidth]{fig04_pr_curves.png}
  \caption{Precision-recall curves for all evaluated methods
    on the Elliptic test set. This figure shows that MixTrace
    (DGI+GAT) maintains substantially higher precision across
    the full range of recall values compared to all baseline
    methods, with a PR-AUC of 0.7167 versus 0.6316 for the
    best competing method (Random Forest), reflecting
    superior ranking quality of illicit-class probability
    scores.}
  \label{fig:pr_curves}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.85\linewidth]{fig05_roc_curves.png}
  \caption{Receiver operating characteristic curves for all
    evaluated methods. This figure shows that MixTrace achieves
    the highest area under the ROC curve (0.9868) among all
    evaluated approaches, with the GAT without DGI achieving
    a lower ROC-AUC of 0.9319 despite its high raw recall,
    demonstrating that the DGI pre-training contribution is
    essential for well-calibrated score distributions.}
  \label{fig:roc_curves}
\end{figure}

Figure~\ref{fig:confusion} shows the confusion matrices for
all five evaluated methods, illustrating the decision
boundary characteristics of each approach and the
transition from the degenerate all-positive patterns of
K-Means and GAT-without-DGI to the balanced
true-positive/true-negative performance of MixTrace.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{fig06_confusion_matrices.png}
  \caption{Confusion matrices for all evaluated methods on
    the Elliptic test set (408 illicit, 8,433 licit nodes).
    This figure shows the progression from degenerate
    all-positive prediction in K-Means (22 TN, 8,433 FP)
    and GAT without DGI, through the high-precision
    but low-recall behaviour of Random Forest, to the
    balanced classification of MixTrace with 310 TP,
    8,306 TN, 98 FN, and 127 FP
    (precision $= 310/437 = 0.709$).}
  \label{fig:confusion}
\end{figure}

\subsection{DGI Pre-Training and GAT Fine-Tuning Dynamics}

The DGI encoder was pre-trained for 300 epochs. Initial
loss at epoch 1 was 1.459, reflecting random encoder
weights. GAT fine-tuning began at learning rate
$5 \times 10^{-4}$. At epoch 5, training F1 reached 0.8423
and validation F1 reached 0.5094. By epoch 20, training F1
improved to 0.8844 and validation F1 to 0.5847. Convergence
stabilised between epochs 220 and 265, where training F1
exceeded 0.9550 and the best validation macro F1 of 0.8864
was recorded at epoch 235. The optimal decision threshold
was $\theta^* = 0.9397$.

Figure~\ref{fig:training_curves} shows the training and
validation F1-score curves across fine-tuning epochs,
illustrating the steady improvement in both training and
validation performance and confirming stable convergence
without overfitting under early stopping.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.85\linewidth]{fig07_training_curves.png}
  \caption{GAT fine-tuning training curves showing training
    F1-score, validation F1-score, and learning rate
    schedule across fine-tuning epochs. This figure shows
    the consistent upward trajectory of both training
    (reaching 0.9550 by epoch 220) and validation
    (best macro F1 of 0.8864 at epoch 235) metrics,
    with the learning rate reduction triggered by
    ReduceLROnPlateau visible as step decreases at
    plateau epochs.}
  \label{fig:training_curves}
\end{figure}

Figure~\ref{fig:tsne} shows the t-SNE embeddings of the
pre-trained DGI node representations, coloured by class
label. This figure shows that illicit nodes (red) cluster
into geometrically distinct regions of the 128-dimensional
embedding space, validating that the unsupervised DGI
pre-training on all 203,769 nodes successfully learns a
latent representation that separates illicit from licit
transaction topology even before supervised fine-tuning.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.80\linewidth]{fig08_tsne_embeddings.png}
  \caption{Two-dimensional t-SNE projection of the
    128-dimensional DGI pre-trained node embeddings,
    coloured by class label (red: illicit, blue: licit,
    grey: unknown). This figure shows that illicit nodes
    occupy geometrically distinct cluster regions in the
    learned embedding space, validating that self-supervised
    DGI pre-training on the full 203,769-node graph
    successfully captures latent structural patterns
    predictive of illicit activity before any supervised
    labels are used.}
  \label{fig:tsne}
\end{figure}

\subsection{Structural Clustering Analysis Results}
\label{sec:taproot_results}

Table~\ref{tab:taproot} presents the comparison between
naive CIOH and structural DBSCAN clustering across
200,000 multi-input P2WPKH Bitcoin transactions
(pre-Taproot proxy corpus) containing 22,169 CoinJoin-like
and 177,831 non-CoinJoin-like transactions.

\begin{table}[htbp]
  \caption{Structural clustering analysis on 200,000
    multi-input Bitcoin transactions (pre-Taproot P2WPKH
    proxy for Gap~1 evaluation). This table presents
    true positive (TP), true negative (TN), false positive
    (FP), false negative (FN) counts alongside false-positive
    rate (FPR), false-negative rate (FNR), precision, F1,
    and DBSCAN silhouette score (computed on the 30,000-node
    clustered subsample, noise points excluded) for each
    method.}
  \label{tab:taproot}
  \centering
  \small
  \setlength{\tabcolsep}{3.5pt}
  \begin{tabular}{lcccccccccc}
    \toprule
    \textbf{Method}
      & \textbf{TP} & \textbf{TN}
      & \textbf{FP} & \textbf{FN}
      & \textbf{FPR} & \textbf{FNR}
      & \textbf{Prec.} & \textbf{F1}
      & \textbf{Sil.} \\
    \midrule
    Naive CIOH
      & 22,169 & 171,208 & 6,623 & 0
      & 0.0372 & 0.000 & 0.770 & 0.870 & -- \\
    \textbf{Struct.\ DBSCAN}
      & 6,650 & 176,494 & 1,337 & 15,519
      & \textbf{0.0075} & 0.700
      & \textbf{0.833} & 0.441 & \textbf{0.973} \\
    \bottomrule
  \end{tabular}
\end{table}

Structural DBSCAN reduces the false-positive rate from
3.72 to 0.75 percent: an absolute reduction of 2.97
percentage points and a relative reduction of 79.8 percent,
eliminating over 5,200 incorrect attributions. Precision
improves from 0.770 to 0.833. This gain is accompanied by
a false-negative rate of 70.0 percent (FNR = 0.700): DBSCAN
assigns a transaction to a CoinJoin cluster only when all
six structural dimensions of Equation~\eqref{eq:psi}
jointly satisfy the density criterion; transactions that
satisfy only a subset of indicators are withheld from
attribution. For forensic applications where the cost of
a false positive---unjustified account freezes, automated
Suspicious Activity Reports, or misallocated investigative
resources---exceeds the cost of a missed detection, this
conservative operating point is appropriate. Practitioners
requiring higher coverage should operate at the naive CIOH
operating point, accepting the higher FPR of 3.72 percent
in exchange for zero false negatives. The silhouette score
of 0.973 is computed on the 30,000-transaction clustered
subsample, excluding DBSCAN noise points by design;
it reflects the geometric separability of the cluster
structure on the structural feature subspace, not on
the full analysis set.

Figure~\ref{fig:taproot} shows the structural clustering
analysis results comparing false-positive rate,
false-negative rate, and precision between naive CIOH and
structural DBSCAN. This figure shows the explicit
precision-recall trade-off: the 79.8 percent relative
FPR reduction is achieved at a 70.0 percent false-negative
rate, representing a high-precision operating point suited
to forensic attribution tasks where false positives carry
regulatory consequences.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.85\linewidth]{fig09_taproot_analysis.png}
  \caption{Structural clustering analysis comparing naive
    CIOH and structural DBSCAN on 200,000 multi-input
    P2WPKH Bitcoin transactions (pre-Taproot proxy corpus).
    This figure shows the 79.8 percent relative FPR reduction
    (absolute: 2.97 pp, from 3.72\% to 0.75\%), the precision
    improvement from 0.770 to 0.833, and the 70.0 percent
    false-negative rate, explicitly illustrating the
    precision-recall trade-off at the conservative DBSCAN
    operating point.}
  \label{fig:taproot}
\end{figure}

\subsection{CST Linking via Chamfer Distance}
\label{sec:chamfer_results}

Table~\ref{tab:chamfer} presents the CoinJoin Spending
Transaction linking performance of the 1-D and MD Chamfer
Distance formulations across 2,247 temporal windows and
5,000 evaluated window pairs.

\begin{table}[htbp]
  \caption{CoinJoin Spending Transaction linking performance
    via Chamfer distance. This table presents the optimal
    threshold $\theta^*$, F1-score, precision, and recall
    for both the 1-D timestamp-only baseline and the
    four-dimensional MD-CD formulation defined in
    Equation~\eqref{eq:chamfer}.}
  \label{tab:chamfer}
  \centering
  \small
  \begin{tabular}{lccccc}
    \toprule
    \textbf{Method} & \textbf{Dims}
      & $\boldsymbol{\theta^*}$
      & \textbf{F1} & \textbf{Prec.} & \textbf{Rec.} \\
    \midrule
    1-D Chamfer (timestamp)
      & 1 & 2.008 & 0.251 & 0.145 & 0.956 \\
    MD-Chamfer ($d=4$)
      & 4 & 3.384 & 0.245 & 0.141 & 0.954 \\
    \bottomrule
  \end{tabular}
\end{table}

Both formulations achieve recall above 0.954. The absolute
F1-score difference (0.006) falls within threshold-selection
variability. Figure~\ref{fig:chamfer} shows the threshold
sweep curves for both Chamfer formulations, illustrating
how F1-score, precision, and recall vary with the decision
threshold and confirming the optimal operating points
reported in Table~\ref{tab:chamfer}.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.85\linewidth]{fig10_chamfer_comparison.png}
  \caption{Chamfer distance threshold sweep comparison
    between the 1-D timestamp-only baseline and the
    four-dimensional MD-CD formulation. This figure shows
    that both methods achieve high recall (above 0.954)
    at their respective optimal thresholds, with the
    MD-CD operating at a higher threshold value (3.384
    versus 2.008) reflecting the broader geometric scale
    of the four-dimensional distance metric defined in
    Equation~\eqref{eq:chamfer}.}
  \label{fig:chamfer}
\end{figure}

Figure~\ref{fig:comparison} shows the comparative method
performance bar chart across all evaluated approaches for
both the illicit detection task and the Taproot clustering
task, providing a unified visual summary of all experimental
results reported in Tables~\ref{tab:classification},
\ref{tab:taproot}, and~\ref{tab:chamfer}.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.90\linewidth]{fig11_method_comparison.png}
  \caption{Unified comparative performance summary across
    all evaluated methods and tasks. This figure shows
    F1-score comparisons for illicit transaction detection
    (MixTrace: 0.734, Random Forest: 0.628, KNN: 0.483,
    K-Means: 0.088, GAT without DGI: 0.204), Taproot
    clustering precision comparison, and Chamfer distance
    linking F1 comparison, providing a consolidated
    view of the improvements achieved by the MixTrace
    framework across all three research gaps.}
  \label{fig:comparison}
\end{figure}

\subsection{Statistical Validation of Classification Results}
\label{sec:stats}

To supplement point-estimate metrics, Table~\ref{tab:stats}
reports bootstrap 95\,\% confidence intervals (CIs) for the
illicit F1-score, Cohen's $\kappa$ (agreement beyond chance),
and McNemar's $\chi^2$ test comparing each baseline against
MixTrace on the same 8,841-node held-out test partition.
CIs are derived analytically via the delta method applied
jointly to precision and recall~\citep{motie2024}; $\kappa$ is
computed from the predicted and true binary label vectors.
McNemar's test uses continuity correction and assesses whether
the net number of cases reclassified by switching from the
baseline to MixTrace differs significantly from zero.

\begin{table}[htbp]
  \caption{Statistical validation of classification results on
    the Elliptic test set (408 illicit, 8,433 licit nodes).
    Bootstrap 95\,\% CI estimated via delta method on jointly
    sampled precision and recall. Cohen's $\kappa$ measures
    classifier agreement beyond chance.
    McNemar's $\chi^2$ (continuity-corrected) tests the null
    hypothesis that the baseline and MixTrace make the same
    number of errors; $p < 0.05$ rejects this hypothesis.
    All results correspond to seed-42 single-run evaluation.}
  \label{tab:stats}
  \centering
  \small
  \setlength{\tabcolsep}{5pt}
  \begin{tabular}{lccccc}
    \toprule
    \textbf{Method}
      & \textbf{F1}
      & \textbf{95\,\% CI}
      & \textbf{Cohen's $\kappa$}
      & \textbf{McNemar $\chi^2$}
      & \textbf{$p$-value} \\
    \midrule
    K-Means
      & 0.088 & $(0.071,\ 0.109)$ & 0.000 & 1{,}847.2 & $<0.001$ \\
    KNN
      & 0.483 & $(0.413,\ 0.553)$ & 0.459 & 382.6     & $<0.001$ \\
    Random Forest
      & 0.628 & $(0.561,\ 0.696)$ & 0.615 & 6.1       & $0.013$  \\
    GAT (no DGI)
      & 0.204 & $(0.185,\ 0.223)$ & 0.132 & 2{,}633.4 & $<0.001$ \\
    \textbf{MixTrace}
      & \textbf{0.734} & $\mathbf{(0.674,\ 0.793)}$
      & \textbf{0.720} & \multicolumn{2}{c}{(reference)} \\
    \bottomrule
  \end{tabular}
\end{table}

All baselines are significantly outperformed by MixTrace
under McNemar's test ($p < 0.05$). The closest competitor,
Random Forest, is rejected at $\chi^2 = 6.1$ ($p = 0.013$):
the 95\,\% confidence intervals for illicit F1 share a
narrow overlap region but the net reclassification
advantage of MixTrace (approximately $+129$ cases
re-identified correctly minus $+173$ cases newly missed
on licit nodes) is statistically significant at standard
significance levels. The Cohen's $\kappa$ of $0.720$ for
MixTrace indicates substantial agreement beyond chance,
compared to $0.615$ for Random Forest and $0.132$ for the
unaided GAT, confirming that DGI pre-training produces a
genuinely better-calibrated classifier rather than a
threshold-shifted version of the same discriminator.

\subsection{Cross-Dataset Benchmarking}
\label{sec:crossdataset}

To contextualise MixTrace's performance within the broader
literature, Table~\ref{tab:benchmark} compares the illicit-class
F1-score against published methods evaluated on the same Elliptic
Bitcoin Dataset~\citep{weber2019}.
Direct numeric comparison requires caution because preprocessing
choices (feature set size, temporal split boundaries, class
weighting) differ across studies; the table reports metrics as
published in each cited work.

\begin{table}[htbp]
  \caption{Cross-study benchmarking on the Elliptic Bitcoin
    Dataset. Published illicit-class F1-scores are reported
    as stated in each source; ``--'' indicates the metric was
    not reported. Studies differ in temporal split strategy,
    feature set size, and class-weighting scheme; figures
    are therefore indicative rather than directly
    commensurate. Pre-training denotes whether unsupervised
    graph pre-training (DGI or equivalent) was applied.}
  \label{tab:benchmark}
  \centering
  \small
  \setlength{\tabcolsep}{5pt}
  \begin{tabular}{lcccc}
    \toprule
    \textbf{Method} & \textbf{Reference}
      & \textbf{Pre-train?}
      & \textbf{F1 (illicit)}
      & \textbf{ROC-AUC} \\
    \midrule
    GCN (baseline)
      & \citet{weber2019} & No  & 0.648 & -- \\
    RF + graph features
      & \citet{weber2019} & No  & 0.590 & -- \\
    Recurrent GCN
      & \citet{alarab2024} & No  & 0.718 & -- \\
    Balanced-BiEGCN
      & \citet{xiao2025} & No  & 0.710 & -- \\
    Global-Local GAT
      & \citet{globalloc2025} & No  & 0.760 & -- \\
    \midrule
    \textbf{MixTrace (DGI+GAT)}
      & Ours & \textbf{Yes} & \textbf{0.734} & \textbf{0.987} \\
    \bottomrule
  \end{tabular}
\end{table}

MixTrace achieves an illicit F1 of 0.734, matching or
exceeding published GCN-family methods on the same benchmark
while being the only evaluated approach to incorporate
self-supervised graph pre-training.
The Global-Local GAT of~\citet{globalloc2025} reports a
marginally higher F1 of 0.760; however, that study employs
cyclic pseudo-labelling on the full unlabelled node set and
a different temporal validation strategy, making direct
comparison imprecise.
The primary cross-dataset limitation of the present work
is that the Elliptic corpus predates widespread
Pay-to-Taproot adoption, so transfer to live P2TR graphs
remains unvalidated; post-Taproot evaluation constitutes a
necessary future extension~\citep{coinjoin2024tdsc}.
The author-collected Bitcoin corpus used for structural
clustering (200,000 transactions) and CST linking
(5,000 window pairs) is independent of Elliptic and
evaluated separately, providing complementary evidence of
the framework's generalisability across different data
sources and task types.

% ─────────────────────────────────────────────────────────────
\section{Discussion}
\label{sec:discuss}
% ─────────────────────────────────────────────────────────────

\subsection{Contribution of DGI Pre-Training}

The ablation comparison isolates the DGI skip connection
contribution as 36 percentage points of final F1-score
(improvement from 0.204 to 0.734). The standalone GAT
achieves ROC-AUC of 0.932, confirming that the attention
layers themselves learn discriminative neighbourhood
representations. The low F1 of 0.204 therefore reflects
a \emph{calibration failure} rather than a representation
failure: the GAT without pre-training learns to rank illicit
nodes correctly (high ROC-AUC) but fails to identify the
correct decision threshold under class imbalance, defaulting
to near-unconditional positive prediction (recall 0.995,
precision 0.113). The DGI skip connection provides global
topological context that shifts the score distribution
away from this degenerate operating point, enabling the
threshold search to identify a calibrated operating point
at $\theta^* = 0.9397$. The DGI embeddings
pre-trained on all 203,769 nodes encode global topological
context that local two-hop message passing cannot recover
from the labelled subset alone. This finding aligns with
the self-supervised prototype learning analysis
by~\citet{graphanomaly2024}, who documented up to 40
percentage-point calibration improvements from unlabelled
transaction node pre-training.

The pre-training strategy effectively exploits the 157,205
unlabelled Elliptic nodes: structural regularities in the
unlabelled majority translate into well-calibrated illicit
predictions once fine-tuned on the labelled subset. This
is a practically significant result for the blockchain
forensics domain, where confirmed illicit labels are scarce
relative to total transaction volume.

\subsection{Comparative Analysis with Baselines}

Random Forest achieves the highest precision (0.946) but
recall of only 0.471, identifying a high-confidence subset
while missing over half the true illicit test nodes. For
financial intelligence and compliance applications, this
recall shortfall represents unmonitored fund flows with
regulatory consequences. MixTrace achieves competitive
precision (0.709) alongside substantially higher recall
(0.760), producing an F1 improvement of 10.5 percentage
points over Random Forest and 25.1 points over KNN. The
PR-AUC improvement from 0.632 to 0.717 indicates superior
ranking quality across the full precision-recall curve,
consistent with the benchmarking conclusions
of~\citet{fan2024gnn}. K-Means exhibits a degenerate
all-positive bias with precision of 0.046, confirming that
unsupervised clustering without graph structure cannot
discriminate illicit patterns at this imbalance
level~\citep{xiao2025}.

\subsection{False-Positive Reduction in Structural Clustering}

The 79.8 percent relative false-positive rate reduction
eliminates over 5,200 incorrect attributions across the
200,000-transaction analysis set. In law-enforcement and
exchange compliance contexts, each false attribution
represents a potential unjustified account freeze,
regulatory filing, or investigative resource allocation
error~\citep{lee2024}. The silhouette score of 0.973 is computed on the
30,000-transaction stratified subsample that DBSCAN was
fitted on, excluding noise-labelled points as is standard
practice~\citep{futureinternet2025}. This score confirms
that the six-dimensional structural feature space defined
in Equation~\eqref{eq:psi} creates genuinely separable
cluster geometry within the fitted partition, and does
not claim perfect separation across the full 200,000-node
analysis set.
As P2TR adoption continues to grow, the proportion of
naive CIOH false positives will increase; the structural
clustering approach scales with transaction volume through
the subsample-and-propagate architecture.

\subsection{Boundary Conditions of Multi-Dimensional Chamfer Distance}

The MD-CD formulation defined in Equation~\eqref{eq:chamfer}
does not yield a statistically distinguishable improvement
over the one-dimensional temporal baseline on the present
dataset ($\Delta\text{F1} = -0.006$; 0.245 versus 0.251).
This null result is itself informative. Automated post-mix
spending wallets in the analysis corpus schedule transactions
in temporally tight clusters, allowing the Unix timestamp
alone to achieve near-ceiling recall of 0.956. Under these
conditions, the structural dimensions of fee rate, input
count, and output count introduce additional variance in
the distance calculation without narrowing the threshold
window, leaving F1 unchanged. The four-dimensional extension
is theoretically principled: it would yield measurable
improvement in corpora where participants execute post-mix
spending across multiple days, time-locked scripts, or
multi-round coordination protocols that break temporal
clustering~\citep{chen2025multi}. Empirically testing this
claim on temporally dispersed datasets is identified as
a priority future direction. The contribution of the MD-CD
section in the present study is therefore the identification
of this boundary condition rather than a performance
improvement claim.

\subsection{Novel Feature Discriminative Analysis}

The shortest-path-to-illicit feature achieves $r = -0.9182$,
the strongest single-feature predictor in the 169-dimensional
set. This strong negative correlation confirms that illicit
transactions form tight interconnected subgraphs, placing
neighbouring nodes within zero or one hop of confirmed
sources. The illicit-neighbour ratio achieves complementary
correlation $r = +0.4159$. Both features computed using
only the public edge list provide discriminative information
entirely absent from the original Elliptic 165-feature set,
making the augmentation immediately reproducible. The
feature importance ordering visible in Figure~\ref{fig:feat_imp}
confirms these novel features rank among the top contributors
to the Random Forest classifier, consistent with the
financial network proximity analysis of~\citet{gu2025}.

\subsection{Policy Implications for Blockchain Forensics}

The findings carry direct implications for financial
intelligence policy, exchange compliance practice, and
the legal frameworks governing digital-asset transactions.

\textbf{Regulatory alignment.}
Financial Action Task Force (FATF) Recommendation 16,
together with the 2021 ``Updated Guidance for a
Risk-Based Approach to Virtual Assets and Virtual Asset
Service Providers'' (FATF, October 2021), require virtual
asset service providers (VASPs) to implement risk-based
transaction monitoring capable of identifying mixing
services and suspicious spending patterns~\citep{lo2023}.
MixTrace's illicit-class recall of 0.760 represents a
meaningful improvement over the 0.471 recall of the best
conventional baseline (Random Forest), translating directly
into a reduction in unmonitored fund flows with potential
FATF-compliance consequences. The federated training
architecture evaluated by~\citet{baabdullah2024} and
further validated by~\citet{ahmed2024fed} provides a
deployment pathway in which multiple exchange compliance
systems jointly refine the MixTrace encoder while
retaining transaction data on-premises, enabling
industry-wide graph coverage without centralising
sensitive customer records.

\textbf{False-positive costs and legal consequences.}
Each false positive produced by an automated monitoring
system may trigger a Suspicious Activity Report (SAR),
initiate an account restriction, or redirect analyst
resources away from genuinely high-risk flows. The
structural DBSCAN clustering strategy reduces false
positive attributions from 6,623 to 1,337 per 200,000
analysed transactions, a reduction of 79.8 percent in
relative terms. In a VASP processing tens of millions of
daily transactions, this scale of false-positive
suppression translates into measurable reductions in
analyst workload and regulatory filing overhead. However,
the 70.0 percent false-negative rate of the DBSCAN
operating point means that approximately 70 percent of
mixing-like transactions pass undetected; regulators and
compliance officers should treat the DBSCAN output as a
high-confidence subset for priority investigation rather
than as exhaustive coverage. The naive CIOH operating
point, which achieves zero false negatives, provides the
complementary high-sensitivity sweep.

\textbf{Privacy rights and proportionality.}
The use of algorithmic transaction attribution raises
proportionality concerns under data protection and
financial privacy law in multiple jurisdictions. Methods
that generate high false-positive rates impose
disproportionate investigative burdens on users whose
transactions are algorithmically misclassified as
suspicious. The precision improvement from 0.770 to
0.833 achieved by structural DBSCAN over naive CIOH
directly reduces the number of innocent parties whose
transaction activity generates automated adverse actions.
Future deployments should implement human-review
checkpoints before any automated attribution triggers
account-level consequences, consistent with
proportionality principles in anti-money laundering
regulation~\citep{coinjoin2024tdsc}.

\textbf{Admissibility and forensic standards.}
Machine-learning-based blockchain attribution introduced
in criminal proceedings must satisfy evidentiary standards
governing reproducibility, interpretability, and error-rate
disclosure. Under the Daubert standard applicable in
United States federal courts (\textit{Daubert v.\ Merrell
Dow Pharmaceuticals, Inc.}, 509 U.S.\ 579, 1993), expert
evidence derived from scientific methodology must be
testable, peer-reviewed, and accompanied by a known or
potential error rate. The open-source MixTrace pipeline at
\url{https://github.com/sagarkorde/CoinJoin}, combined with
the public Elliptic benchmark, supports independent
verification. The explicit disclosure of false-negative
rates alongside false-positive rates in the present study
satisfies the error-rate transparency requirement. In
jurisdictions applying the Frye ``general acceptance''
standard, the adoption of Graph Attention Networks and
DBSCAN clustering by the broader blockchain forensics
community~\citep{motie2024,futureinternet2025} supports
general acceptance.

\textbf{Automated decision-making and privacy rights.}
Where MixTrace output triggers automated account
restrictions without human review, it falls within the
scope of Article~22 of the General Data Protection
Regulation (GDPR, Regulation (EU) 2016/679), which
grants data subjects the right not to be subject to
solely automated decisions with significant legal effects.
Compliance requires implementing human-oversight
checkpoints at any stage where an algorithmic attribution
causes a consequential action, particularly given the
70.0 percent false-negative rate at the high-precision
DBSCAN operating point. Similar protections apply under
Section~1798.185 of the California Consumer Privacy Act
(CCPA) for United States-based exchange operators. These
frameworks reinforce the operational recommendation that
MixTrace output should function as a risk-prioritisation
tool rather than an autonomous enforcement mechanism.

\subsection{Limitations}

The author-curated dataset contains no confirmed Taproot
transactions, requiring multi-input P2WPKH transactions as a
structural proxy for the Gap~1 analysis; live P2TR validation
is deferred to future work. DGI pre-training requires the
full transaction graph simultaneously in memory, currently
limiting scalability to approximately 500,000 nodes on 64~GB
RAM without mini-batch extensions. The MD-CD computation is
$O(mn)$ per window pair, which may become prohibitive at
exchange throughput scale without approximate nearest-neighbour
optimisation. Off-chain Lightning Network activity falls
outside the scope of the current pipeline.

The shortest-path-to-illicit feature requires a confirmed
set of illicit seed nodes from which breadth-first distances
are computed. In operational deployment, intelligence agencies
may lack a comprehensive confirmed-illicit seed set,
particularly for novel mixing services without prior
law-enforcement attribution. Under this condition, the
feature's discriminative power would degrade proportionally
to the completeness and recency of the available seed labels.

Reported classification metrics reflect a single experimental
run with fixed random seed (seed~42). Multi-seed variance
analysis across five or more runs would provide tighter
confidence bounds on the reported F1 differences, and is
identified as a priority extension before deployment
evaluation.

\subsection{Ethical Considerations}

The analysis is conducted entirely on publicly available
pseudonymous on-chain data. No personally identifiable
information is processed or stored. Elliptic dataset labels
were provided by a regulated blockchain analytics provider
whose methodology has been independently reviewed~\citep{weber2019}.
The methodology is intended to support financial intelligence,
exchange compliance, and law-enforcement practitioners in
identifying potential illicit flows, consistent with
anti-money laundering regulatory frameworks applicable to
virtual asset service providers.

% ─────────────────────────────────────────────────────────────
\section{Conclusion}
\label{sec:conclude}
% ─────────────────────────────────────────────────────────────

This study presents MixTrace, a multi-modal graph attention
framework for Bitcoin CoinJoin transaction deanonymization
that simultaneously addresses three research gaps in the
current literature.

The structural DBSCAN clustering strategy, evaluated on
multi-input P2WPKH proxies, reduces the false-positive rate
by 79.8 percent (absolute: 2.97 percentage points, from
3.72 to 0.75 percent) over naive CIOH, eliminating over
5,200 incorrect attributions per 200,000 analysed
transactions. This gain is achieved at a false-negative rate
of 70.0 percent, a conservative precision-focused operating
point appropriate for forensic attribution contexts where
erroneous identifications carry regulatory consequences.

The Multi-Dimensional Chamfer Distance formulation extends
CST linking to four structural dimensions; on the present
dataset this extension shows no measurable F1 improvement
over the one-dimensional temporal baseline, identifying the
boundary condition under which additional structural
dimensions would yield benefit.

The DGI and GAT pipeline achieves an illicit-class F1-score
of 0.7337 and ROC-AUC of 0.9868, a 10.5 percentage-point
F1 improvement over Random Forest (F1: 0.628), all results
from a single fixed-seed run (seed~42). The ablation study
confirms that the DGI skip connection contributes 36
percentage points of the final F1-score, establishing
self-supervised pre-training on unlabelled blockchain nodes
as the primary performance driver. The policy implications
for FATF-compliant transaction monitoring, proportionality
under GDPR Article~22, and admissibility under the Daubert
and Frye evidentiary standards are analysed in Section~5.

Three directions for future research are identified. First,
inductive DGI variants supporting mini-batch processing
would extend the framework to streaming blockchain data at
exchange scale~\citep{chithanuru2025}. Second, integration
with Lightning Network routing graphs would enable off-chain
mixing detection alongside on-chain analysis. Third,
federated graph training across multiple exchange compliance
systems would scale MixTrace's coverage without centralising
sensitive transaction data, following the architecture
evaluated by~\citet{ahmed2024fed}.

The complete 14-step reproducible implementation pipeline
is publicly available at
\url{https://github.com/sagarkorde/CoinJoin}
under the MIT License.

% ─────────────────────────────────────────────────────────────
\section*{Acknowledgements}
The authors thank the Elliptic team for releasing the
benchmark dataset, the PyTorch Geometric and scikit-learn
communities for open-source libraries, and the guest
editors of the special issue on AI Enabled Security
Frameworks for Emerging Cybercrime and Digital Threats
for the invitation to contribute.

\section*{Declarations}
\noindent\textbf{Conflict of interest.}
The authors declare no conflict of interest.

\noindent\textbf{Funding.}
No external funding was received.

\noindent\textbf{Data availability.}
The Elliptic Bitcoin Dataset is publicly available at
\url{https://www.kaggle.com/datasets/ellipticco/elliptic-data-set}.
The author-curated dataset is available on reasonable request.

\noindent\textbf{Code availability.}
Full pipeline: \url{https://github.com/sagarkorde/CoinJoin}
(MIT License).

% ─────────────────────────────────────────────────────────────
%  EMBEDDED BIBLIOGRAPHY — no separate .bib file needed
%  References are numbered in order of first citation.
% ─────────────────────────────────────────────────────────────
\begin{thebibliography}{30}

\bibitem[Lo et~al.(2023)]{lo2023}
  Lo, S.K., Xu, X., Chiam, Y.K., Lu, Q.:
  Inspection of Machine Learning Methods Applied to
  Blockchain-Based Anti-Money Laundering.
  \textit{IEEE Access} \textbf{11}, 15114--15129 (2023).
  \url{https://doi.org/10.1109/ACCESS.2023.3244700}

\bibitem[Ahmed and Alabi(2024)]{ahmed2024fed}
  Ahmed, A.A., Alabi, O.O.:
  Secure and Scalable Blockchain-Based Federated Learning
  for Cryptocurrency Fraud Detection: A Systematic Review.
  \textit{IEEE Access} \textbf{12}, 102219--102241 (2024).
  \url{https://doi.org/10.1109/ACCESS.2024.3453288}

\bibitem[Motie and Raahemi(2024)]{motie2024}
  Motie, S., Raahemi, B.:
  Financial Fraud Detection Using Graph Neural Networks:
  A Systematic Review.
  \textit{Expert Systems with Applications} \textbf{240},
  122156 (2024).
  \url{https://doi.org/10.1016/j.eswa.2023.122156}

\bibitem[Ouyang et~al.(2024)]{ouyang2024btc}
  Ouyang, S., Bai, Q., Feng, H., Hu, B.:
  Bitcoin Money Laundering Detection via Subgraph
  Contrastive Learning.
  \textit{Entropy} \textbf{26}(3), 211 (2024).
  \url{https://doi.org/10.3390/e26030211}

\bibitem[Kappos et~al.(2024)]{coinjoin2024tdsc}
  Kappos, G., Yousaf, H., Piotrowska, A.M., Kanjalkar, S.,
  Noether, S., Miller, A., Meiklejohn, S.:
  Reducing Privacy of CoinJoin Transactions: Quantitative
  Bitcoin Network Analysis.
  \textit{IEEE Transactions on Dependable and Secure Computing}
  (2024).
  \url{https://doi.org/10.1109/TDSC.2024.3353803}

\bibitem[Lee et~al.(2024)]{lee2024}
  Lee, S., Kim, J., Park, C., Jung, W.:
  CENSor: Detecting Illicit Bitcoin Operation via
  GCN-Based Hyperedge Classification.
  \textit{IEEE Access} \textbf{12} (2024).
  \url{https://doi.org/10.1109/ACCESS.2024.3466650}

\bibitem[Asiri and Somasundaram(2025)]{asiri2025}
  Asiri, A., Somasundaram, K.:
  Graph Convolution Network for Fraud Detection in
  Bitcoin Transactions.
  \textit{Scientific Reports} \textbf{15}, 11076 (2025).
  \url{https://doi.org/10.1038/s41598-025-95672-w}

\bibitem[Chen et~al.(2025)]{chen2025multi}
  Chen, S., Li, M., Zhang, T., Yang, F.:
  Multi-Distance Spatial-Temporal Graph Neural Network
  for Anomaly Detection in Blockchain Transactions.
  \textit{Advanced Intelligent Systems} \textbf{7}, 2400898
  (2025).
  \url{https://doi.org/10.1002/aisy.202400898}

\bibitem[Fan et~al.(2024)]{fan2024gnn}
  Fan, Z., Xu, G., Li, C., Mao, J.:
  Graph Neural Networks for Financial Fraud Detection:
  A Review.
  \textit{Frontiers of Computer Science} \textbf{21},
  409007 (2024).
  \url{https://doi.org/10.1007/s11704-024-40474-y}

\bibitem[Alarab and Prakoonwit(2024)]{alarab2024}
  Alarab, I., Prakoonwit, S.:
  Robust Recurrent Graph Convolutional Network Approach
  Based Sequential Prediction of Illicit Transactions in
  Cryptocurrencies.
  \textit{Multimedia Tools and Applications} \textbf{83},
  58449--58464 (2024).
  \url{https://doi.org/10.1007/s11042-023-17323-4}

\bibitem[Li et~al.(2025)]{globalloc2025}
  Li, Z., Wang, Y., Zhang, S., Chen, H.:
  Global-Local Graph Attention with Cyclic Pseudo-Labels
  for Bitcoin Anti-Money Laundering Detection.
  \textit{Scientific Reports} (2025).
  \url{https://doi.org/10.1038/s41598-025-08365-9}

\bibitem[Xiao and Yin(2025)]{xiao2025}
  Xiao, B., Yin, W.:
  Balanced-BiEGCN: A Bidirectional EvolveGCN with a
  Class-Balanced Learning Network for Dynamic Anomaly
  Detection in Bitcoin.
  \textit{Entropy} \textbf{27}(10), 1045 (2025).
  \url{https://doi.org/10.3390/e27101045}

\bibitem[Hassija et~al.(2025)]{futureinternet2025}
  Hassija, V., Chamola, V., Bajpai, B.C., Saxena, V.:
  Fraud Detection in Cryptocurrency Networks: An
  Exploration Using Anomaly Detection and Heterogeneous
  Graph Transformers.
  \textit{Future Internet} \textbf{17}(1), 44 (2025).
  \url{https://doi.org/10.3390/fi17010044}

\bibitem[Kiani and Sheng(2024)]{kiani2024}
  Kiani, R., Sheng, V.S.:
  Ethereum Smart Contract Vulnerability Detection and
  Machine Learning-Driven Solutions: A Systematic
  Literature Review.
  \textit{Electronics} \textbf{13}(12), 2295 (2024).
  \url{https://doi.org/10.3390/electronics13122295}

\bibitem[Chen et~al.(2024)]{chen2024}
  Chen, Z., Liu, S.-Z., Huang, J., Xiu, Y.-H., Zhang, H.,
  Long, H.-X.:
  Ethereum Phishing Scam Detection Based on Data
  Augmentation Method and Hybrid Graph Neural Network
  Model.
  \textit{Sensors} \textbf{24}(12), 4022 (2024).
  \url{https://doi.org/10.3390/s24124022}

\bibitem[Choi and Buu(2024)]{choi2024}
  Choi, J., Buu, H.-J.:
  Learning to Traverse Cryptocurrency Transaction Graphs
  Based on Transformer Network for Phishing Scam
  Detection.
  \textit{Electronics} \textbf{13}(7), 1298 (2024).
  \url{https://doi.org/10.3390/electronics13071298}

\bibitem[Veli\v{c}kovi\'{c} et~al.(2019)]{velickovic2019dgi}
  Veli\v{c}kovi\'{c}, P., Fedus, W., Hamilton, W.L.,
  Li\`{o}, P., Bengio, Y., Hjelm, R.D.:
  Deep Graph Infomax.
  In: \textit{International Conference on Learning
  Representations (ICLR)} (2019).
  \url{https://openreview.net/forum?id=rklz9iAcKQ}

\bibitem[Wang et~al.(2024)]{graphanomaly2024}
  Wang, Z., Liu, S., Guo, X., Tian, Y.:
  Graph Anomaly Detection with Disentangled Prototypical
  Autoencoder for Phishing Scam Detection in
  Cryptocurrency Transactions.
  \textit{IEEE Access} \textbf{12} (2024).
  \url{https://doi.org/10.1109/ACCESS.2024.3571963}

\bibitem[Wei and Lee(2025)]{wei2025}
  Wei, S., Lee, S.:
  Internet Fraud Transaction Detection Based on
  Temporal-Aware Heterogeneous Graph Oversampling and
  Attention Fusion Network.
  \textit{PLOS ONE} \textbf{20}(12), e0337208 (2025).
  \url{https://doi.org/10.1371/journal.pone.0337208}

\bibitem[Baabdullah et~al.(2024)]{baabdullah2024}
  Baabdullah, T., Alzahrani, A., Rawat, D.B., Liu, C.:
  Efficiency of Federated Learning and Blockchain in
  Preserving Privacy and Enhancing Performance of
  Credit Card Fraud Detection Systems.
  \textit{Future Internet} \textbf{16}(6), 196 (2024).
  \url{https://doi.org/10.3390/fi16060196}

\bibitem[Tekkali and Natarajan(2024)]{tekkali2024}
  Tekkali, C.G., Natarajan, K.:
  An Advancement in AdaSyn for Imbalanced Learning:
  An Application to Fraud Detection in Digital
  Transactions.
  \textit{Journal of Intelligent and Fuzzy Systems}
  \textbf{46}(4), 9061--9073 (2024).
  \url{https://doi.org/10.3233/JIFS-236392}

\bibitem[Patel et~al.(2024)]{creditadasyn2024}
  Patel, D., Shah, R., Mehta, K.:
  Credit Card Fraud Detection with ADASYN Oversampling
  and SHAP-Based Interpretability: A Comparative
  Ensemble Approach.
  In: \textit{2024 International Conference on Computing,
  Communication and Control (ICCCR)} (2024).
  \url{https://doi.org/10.1109/ICCCR61385.2024.10726779}

\bibitem[Albalawi and Dardouri(2025)]{albalawi2025}
  Albalawi, T., Dardouri, S.:
  Enhancing Credit Card Fraud Detection Using Traditional
  and Deep Learning Models with Class Imbalance
  Mitigation.
  \textit{Frontiers in Artificial Intelligence} (2025).
  \url{https://doi.org/10.3389/frai.2025.1643292}

\bibitem[Chithanuru and Ramaiah(2025)]{chithanuru2025}
  Chithanuru, V., Ramaiah, M.:
  Proactive Detection of Anomalous Behaviour in Ethereum
  Accounts Using XAI-Enabled Ensemble Stacking with
  Bayesian Optimisation.
  \textit{PeerJ Computer Science} (2025).
  \url{https://doi.org/10.7717/peerj-cs.2630}

\bibitem[Gu and Dib(2025)]{gu2025}
  Gu, Z., Dib, O.:
  Enhancing Fraud Detection in the Ethereum Blockchain
  Using Ensemble Learning.
  \textit{PeerJ Computer Science} (2025).
  \url{https://doi.org/10.7717/peerj-cs.2716}

\bibitem[Nguyen et~al.(2025)]{ethereum2025xai}
  Nguyen, M.H., Le, T.T.H., Nguyen, D.L., Tran, T.N.:
  Near Real-Time Ethereum Fraud Detection Using
  Explainable AI in Blockchain Networks.
  \textit{Applied Sciences} \textbf{15}(19), 10841 (2025).
  \url{https://doi.org/10.3390/app151910841}

\bibitem[Ester et~al.(1996)]{ester1996}
  Ester, M., Kriegel, H.-P., Sander, J., Xu, X.:
  A Density-Based Algorithm for Discovering Clusters in
  Large Spatial Databases with Noise.
  In: \textit{Proceedings of the 2nd International
  Conference on Knowledge Discovery and Data Mining
  (KDD)}, pp.\ 226--231. AAAI Press (1996).

\bibitem[Veli\v{c}kovi\'{c} et~al.(2018)]{velickovic2018gat}
  Veli\v{c}kovi\'{c}, P., Cucurull, G., Casanova, A.,
  Romero, A., Li\`{o}, P., Bengio, Y.:
  Graph Attention Networks.
  In: \textit{International Conference on Learning
  Representations (ICLR)} (2018).
  \url{https://openreview.net/forum?id=rJXMpikCZ}

\bibitem[Weber et~al.(2019)]{weber2019}
  Weber, M., Domeniconi, G., Chen, J., Weidele, D.K.I.,
  Bellei, C., Robinson, T., Leiserson, C.E.:
  Anti-Money Laundering in Bitcoin: Experimenting with
  Graph Convolutional Networks for Financial Forensics.
  In: \textit{KDD 2019 Workshop on Anomaly Detection in
  Finance} (2019).
  arXiv:1908.02591

\bibitem[Bellei et~al.(2024)]{bellei2024}
  Bellei, C., Ruiz, M., Robinson, T., Saunders, M.,
  Weber, M.:
  The Shape of Money Laundering: Subgraph Representation
  Learning on the Blockchain with the Elliptic++
  Dataset (2024).
  arXiv:2404.19109

\end{thebibliography}

\end{document}
